openapi:
  blockerService:
    base-path: /

spring:
  application:
    name: blocker-service

  cloud:
    config:
      enabled: false

  rabbitmq:
    listener:
      direct:
        auto-startup: false

eureka:
  client:
    enabled: false
