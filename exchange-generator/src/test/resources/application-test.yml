spring:
  application:
    name: exchange-generator

  cloud:
    config:
      enabled: false

  rabbitmq:
    listener:
      direct:
        auto-startup: false


eureka:
  client:
    enabled: false

  rest:
    exchange-service:
      url: http://localhost:7087